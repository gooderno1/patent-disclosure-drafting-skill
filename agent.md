# AGENTS 指南

本文件用于指导后续 AI / 开发者在 `AIScientist` 项目中的协作方式。它不是一次性任务说明，也不固定绑定某个具体实验、模型或论文主题；每次开发仍需根据任务目标自行确认需要阅读的 README、配置、脚本、数据样例和现有实现。

## 1. 基本原则

- 全程使用中文交流、说明和用户可见文案；代码、变量名、命令和上游英文术语保持项目原风格。
- 文档先行，先理解 README、配置文件、入口脚本、模型调用逻辑和现有实验流程，再动手改代码。
- 以事实来源为准：README、`bfts_config.yaml`、脚本参数、Mock/样例 idea 文件和现有代码之间若有冲突，先指出并统一口径。
- 不臆测模型能力、API 字段、实验状态、论文生成流程、成本和业务规则；缺少依据时先查项目资料，资料没有再补约定或向用户确认。
- 尊重 `AIScientist` 当前技术栈和目录结构，优先沿用项目已有模式，不随意引入新框架或大范围重构。
- 本项目会执行 LLM 生成的代码。涉及实验运行、文件系统、网络访问、进程管理和第三方包安装时，默认按高风险操作处理。

## 2. 工作方式

- 开发前先执行 `git status`，确认当前工作区状态。
- 每个任务开始时先判断：这是认证接入、模型支持、实验流程调整、文档修订、依赖环境、结果分析，还是工程清理。
- 根据任务类型阅读相关文件，不把某一份旧说明当成永久唯一入口。
- 改动应保持聚焦，只处理当前任务相关的文件和行为。
- 涉及 CLI 参数、模型列表、API 认证、配置字段、输出目录或实验流程变化时，同步更新对应文档，避免文档和实现分裂。
- 不要在未确认成本、API Key、GPU 资源和运行时长的情况下启动完整 AIScientist 实验。
- 代码完成后至少执行轻量验证；例如 `python -m py_compile`、入口脚本 `--help`、认证解析单测式脚本。影响实验主流程时，应补充更贴近真实路径的验证。
- 每次完成有效修改后都要进行 Git 同步，至少包括：检查变更、更新开发记录、提交 commit；如远端可用，再执行 push。
- 除非用户明确要求只保留工作区改动，否则每次任务结束前都必须提交当前任务相关变更，不能把有效修改长期停留在未提交状态。

## 3. 认证与密钥

- 不要把 API Key、OAuth token、Codex 登录文件、AWS 凭据或任何秘密写入仓库。
- OpenAI 路径分为两类：`OPENAI_API_KEY` 走直接 OpenAI API；Codex 登录走独立的 Codex backend。修改认证逻辑时必须明确区分两条通道，不要把 Codex token 当作普通 API key 使用。
- 其他密钥按来源区分：`S2_API_KEY` 用于 Semantic Scholar，`GEMINI_API_KEY` 用于 Gemini，AWS Bedrock 使用 AWS 环境变量，HuggingFace 使用 `HUGGINGFACE_API_KEY`。
- 修改认证逻辑时，必须保留原有环境变量路径，新增路径应是兼容增强，不应破坏已有用户。
- 验证认证逻辑时，优先用临时假凭据测试解析，不在日志中打印真实 token。

## 4. 版本号与 Git 同步

### 4.1 版本号规则

- 开发迭代版本：`vX.Y.Z-dev.N`
- 正式发布版本：`vX.Y.Z`
- `X`：重大结构、技术栈或产品方向变化。
- `Y`：新增核心流程、重要模型能力、主要实验能力。
- `Z`：缺陷修复、小范围增强、文档完善。
- `N`：同一个正式版本前的开发迭代序号，从 `1` 递增。

示例：

- `v0.1.0-dev.1 docs: 建立项目协作规范`
- `v0.1.0-dev.2 feat(auth): 支持 Codex 登录凭据`
- `v0.1.0 release: 首个可复现实验版本`

### 4.2 Git 同步要求

- 开发前执行 `git status`。
- 如果存在远端且当前任务需要基于最新代码开发，先执行 `git pull --rebase`。
- 提交时只提交本次任务相关文件，不混入无关生成物、依赖目录、实验输出或历史脏文件。
- commit message 必须带开发版本号，格式：`vX.Y.Z-dev.N type(scope): description`。
- 常用类型：`feat`、`fix`、`docs`、`refactor`、`chore`、`test`。
- 若配置了远端，提交后执行 `git push`。
- 每次任务至少产生一个与当前任务对应的 commit；不要把多个无关任务混在同一个提交里。
- 如果本地存在与当前任务无关的改动，保持不动，不要回滚；如同一文件存在交叉改动，先理解再继续。

## 5. 开发记录

- 每次有效更新都必须更新 `DEVELOPMENT_LOG.md`。
- 开发记录要写清楚版本号、日期、变更类型、开发原因、实现方式、当前结果和验证方式。
- 开发记录必须倒序排列，最新版本放在最上方，方便接手者快速查看当前状态。
- 遗留问题不是必填项；只有确实存在未解决问题、风险或后续动作时才写。
- 记录不能只写“优化”“更新”这类空泛描述，必须能让后续接手者知道实际做了什么。

建议格式：

```md
## [YYYY-MM-DD] vX.Y.Z-dev.N type: 简短说明

- 开发原因：说明本次开发是新增功能、修复 bug、解决 issue、调整规范，还是补齐文档。
- 实现方式：说明具体怎么做，涉及哪些关键文件或模块。
- 当前结果：说明本次完成后的状态和结论。
- 验证方式：说明执行了哪些检查或测试。
- 遗留问题：如有，说明未完成内容、风险或后续建议。
```

## 6. Release 记录

- 每次正式 Release 都必须更新 `RELEASE_NOTES.md`。
- Release 说明必须列出本次 Release 包含哪些开发版本。
- Release 说明必须写清楚主要变更、修复内容、验证结果和升级注意事项。
- 已知问题不是必填项；只有确实存在仍需关注的问题时才写。
- 如果某个开发版本没有进入本次 Release，要明确说明原因。

建议格式：

```md
## vX.Y.Z - YYYY-MM-DD

- 包含版本：`vX.Y.Z-dev.1`、`vX.Y.Z-dev.2`
- 主要变更：说明本次发布包含的功能、模型支持或实验流程变化。
- 修复内容：说明修复了哪些问题。
- 验证结果：说明依赖安装、脚本检查、实验 dry run 或测试情况。
- 已知问题：如有，说明仍需后续处理的风险。
```

## 7. 中间资料目录

- 预留 `local_dev_work/` 存放开发过程中的临时分析、草稿、中间结果和排查记录。
- `local_dev_work/` 默认不参与 Git 同步。
- 需要长期保留或交付的内容，应整理后放入正式文档或合适源码目录，再纳入 Git。
- 不要把临时文件散落在仓库根目录。

## 8. 交付标准

- Python 文件语法检查通过，不能留下明显导入错误或入口脚本参数错误。
- 用户可见说明和文档应清晰、中文优先。
- 数据展示、实验参数和模型选择必须能追溯到 README、配置、样例文件、上游代码或明确约定。
- 重要流程应具备可验证路径，而不是只改静态文档或只做未运行的假设。
- 结果说明要交代改了什么、验证了什么、还有什么风险。

## 9. 工程约束

- 不要把生成产物、临时文件、依赖目录、实验输出、模型缓存或 `.venv/` 当作源码修改。
- 不要在根目录随意创建散乱文件。
- 不要在缺少上下文时重写架构或替换技术栈。
- 不要为了快速展示而硬编码一套脱离配置、样例 idea 和上游流程的新实验模型。
- 不要扩展已经确定为旧版或 legacy 的路径，除非当前任务明确要求处理它们。
- 修改依赖时要说明原因，并考虑 Windows、Linux、CUDA 和 Python 版本差异。

## 10. 修改冲突处理

当发现 README、配置、样例数据、实验输出或代码互相不一致时，按下面方式处理：

1. 先指出冲突点。
2. 判断哪个来源更接近当前任务目标。
3. 必要时补充或修正文档。
4. 再做代码修改。

如果冲突会影响产品方向、实验结论、成本、外部 API 使用或数据含义，先向用户确认，不要自行决定。
