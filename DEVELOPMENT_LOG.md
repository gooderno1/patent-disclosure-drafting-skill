## [2026-05-22] v0.1.0-dev.4 docs: 固化前置查重与双轮检索流程

- 开发原因：现有 skill 将检索更多表述为后置评估环节，未明确“题目确定前先查重、初稿完成后再复检”的双轮流程，容易导致题目方向和重点保护特征定得过早。
- 实现方式：更新 `SKILL.md`、`references/workflow.md`、`references/patentability-search.md`、`references/review-checklist.md` 和 `assets/search-report-template.md`，把首轮前置检索、第二轮初稿复检、轮次标记和回改要求写入主流程、检索规范、检查清单与报告模板。
- 当前结果：skill 已明确要求完整交底流程至少执行两轮检索，用检索结果决定题目方向和重点，而不是只在结尾补一份风险报告。
- 验证方式：检查主流程、检索规范、模板和审查清单是否统一体现 `题目初选` 与 `初稿复核` 两个检索节点。

## [2026-05-21] v0.1.0-dev.3 docs: 补充正式附图文体与 docx 编号规则

- 开发原因：在真实专利交底书交付过程中发现两类高频问题：一是 `2.8/2.9` 图示说明容易残留“建议按……绘制”这类内部制图措辞；二是 `docx` 中独立有序列表会因 Word 自动编号续号而出现串号。
- 实现方式：更新 `SKILL.md`、`references/workflow.md`、`references/review-checklist.md`、`references/docx-export.md` 和 `assets/disclosure-template.md`，把正式附图表述、制图指令清理要求、列表显式编号策略和导出核验点写入 skill。
- 当前结果：skill 已覆盖本轮专利交底书实战中新增沉淀出的正式交付规则，可直接约束后续专利写作与 `docx` 同步。
- 验证方式：检查更新后的 skill 文档是否同时覆盖主流程、模板、最终审查和 `docx` 导出四个层面。

## [2026-05-21] v0.1.0-dev.2 docs: 修正协作指南项目名称

- 开发原因：上一版 `agent.md` 按用户提供文本落地时保留了不属于当前仓库的项目名，需要改回本项目名称。
- 实现方式：将 `agent.md` 中的项目名称统一修正为 `patent-disclosure-drafting`，并同步调整一处与当前仓库更匹配的工作流描述。
- 当前结果：协作指南已与当前仓库名称对齐，不再直接引用旧项目名。
- 验证方式：执行文本检索确认 `agent.md` 与开发记录中的残留引用范围。

## [2026-05-21] v0.1.0-dev.1 docs: 建立 AGENTS 协作规范

- 开发原因：为仓库补充后续 AI / 开发者协作规范，并把当前目录纳入 Git 管理。
- 实现方式：新建 `agent.md` 写入协作指南；新增 `.gitignore` 忽略临时开发目录和常见 Python 产物；建立本开发记录作为首条版本记录。
- 当前结果：仓库具备可追踪的协作文档、开发记录和基础忽略规则，可继续进行后续版本化开发。
- 验证方式：执行 `git status --short --branch` 检查工作区与待提交文件范围。
- 遗留问题：初始版本残留了外部项目名称；该问题已在 `v0.1.0-dev.2` 修正。
